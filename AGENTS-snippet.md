Add this line to `~/.codex/AGENTS.md` to make the skill the default workflow
for implementation and repository-review tasks:

```markdown
For repository audits, reviews, features, bug fixes, refactors, and test fixes, apply `$efficient-implementation`.
```
